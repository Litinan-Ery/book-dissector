# 图书拆解器 —— 需求文档

- 版本：v0.2.1（本地管理能力需求补充版）
- 日期：2026-08-09
- 状态：v0.2.1 已完成；M8 代码、58 项全量自动化、ego lite/Finder 真实操作与服务重启验收通过

## 0. 本版原则

本版以 v0.1 的产品体验和需求范围为主体，保留原有五项改进，并补充三项本地管理能力：

1. 识别表格、图片、公式、代码块；当前不能处理时必须告警。
2. 支持持久化队列、检查点、取消、恢复、缓存和失败单元重试。
3. 默认使用一键端到端任务，同时保持内部阶段可见。
4. 开始前展示 token、API 调用量、时间和成本估算。
5. 明确必要正文片段会发送给 DeepSeek，并在首次调用前要求用户确认。
6. 支持删除任务队列中的指定任务，不误删书籍和最终导出。
7. 支持删除书库中的指定书籍条目，并明确关联数据的清理边界。
8. 完成态任务支持直接打开导出文件所在文件夹。

新增管理能力不改变“一键拆解”的主流程，也不引入批量删除、自动清理或复杂回收站管理。

除此以外，v0.2 曾引入的自适应预算、全书定向扫描、强制结构化知识单元、强制原文锚点、全书语义去重、可逆删减映射、质量硬门禁、诊断稿等需求均回滚，不再作为本版产品体验或发布前置条件。

## 1. 项目概述

### 1.1 背景

现代人的阅读时间碎片化，而图书中往往包含大量与主旨无关的内容（版权页、目录、尾注、参考文献、冗余叙述等），导致“读完一本书”的时间成本很高。用户希望在不遗漏核心知识的前提下，大幅缩短阅读时间。

### 1.2 产品定位

图书拆解器是一个**书籍精华提取工具**：将用户导入的电子书自动删减无关内容、压缩核心章节，仅保留核心观点和最关键、最生动的例子，最终输出一份精华版 Markdown 文档。

### 1.3 目标

- 帮助用户节约阅读时间。
- 提高知识获取效率。
- 减少任务中断、失败重跑和重复调用造成的等待与成本。
- 允许用户清理无用任务和已导入书籍，避免任务队列与书库长期堆积。
- 允许用户从已完成任务直接定位最终导出，减少查找本地文件的操作成本。
- 产出可长期保存、可检索、可分享的精华文档（MD 格式）。

### 1.4 非目标（本期不做）

- 不做书籍翻译。
- 不做全文检索 / 知识图谱。
- 不做多本书的横向对比总结。
- 不做社交分享功能。
- 不做笔记 / 批注 / 书签功能（精华稿的笔记请用户在外部工具中自行完成）。
- 不做自适应压缩预算、全书语义去重、强制原文锚点或知识单元分类。
- 不做批量删除、定时自动清理或自定义数据保留周期。

## 2. 目标用户

| 用户类型 | 典型场景 |
| --- | --- |
| 忙碌的知识工作者 | 需要在短时间内掌握一本书的核心观点 |
| 学生 / 备考者 | 快速回顾教材或参考书的要点 |
| 泛读爱好者 | 大量浏览书籍，只对少数书精读 |

## 3. 术语定义

- **无关内容**：与书籍主旨无关的部分，如版权页、目录、序言（视情况）、尾注、参考文献、索引、空白页、广告页等。
- **核心章节**：承载书籍主旨论述的正文章节。
- **核心观点**：作者的核心论点、结论、方法论，以及支撑论点的关键论据。
- **关键例子**：最能说明论点、最具说服力或最生动的案例、故事、数据。
- **任务检查点**：任务完成一个可独立处理单元后保存的进度，用于取消后继续、服务重启恢复和失败单元重试。
- **任务记录**：任务队列中的一条端到端拆解任务，包含状态、进度、检查点、错误和任务级中间结果。
- **书库条目**：图书拆解器书库中的一本已导入书籍及其应用内副本、元数据、提取文本和书籍级中间产物；不等同于用户导入前的原始文件。
- **精华版 MD**：本项目最终产出的 Markdown 文档。

## 4. 功能需求

### 4.1 书籍导入

- FR-1.1：支持导入常见电子书格式：EPUB、PDF、TXT、Markdown。
- FR-1.2：导入后展示书籍基本信息（书名、作者、格式、页数/字数、导入时间）。
- FR-1.3：支持批量导入多本书，并进入持久化处理队列。
- FR-1.4：导入失败的书籍需给出明确失败原因（如格式不支持、文件损坏）。

### 4.2 无关内容删减

- FR-2.1：自动识别并剔除版权页、出版信息页。
- FR-2.2：自动识别并剔除目录页（生成书签用，不进入正文精华）。
- FR-2.3：自动识别并剔除尾注、脚注、参考文献、索引部分。
- FR-2.4：自动识别并剔除空白页、重复页、广告页。
- FR-2.5：序言、导言、后记是否保留，可由用户选择（默认保留序言/导言，剔除后记以外的附属内容）。
- FR-2.6：删减结果应可预览，用户可手动撤销误删（将某段内容恢复到精华稿中）。

### 4.3 核心内容压缩

- FR-3.1：对保留下来的核心章节进行大幅压缩，目标保留原篇幅的 **15% 左右**（即压缩约 85%）；不同书籍类型采用差异化目标比例。
- FR-3.2：压缩后保留：
  - 核心观点及其逻辑关系（论点 → 论据 → 结论）。
  - 最关键、最生动的例子（每个核心观点至少保留 1 个代表例子）。
  - 关键术语及其首次出现时的简短解释。
- FR-3.3：压缩结果应保持原文的章节层级结构（章 → 节 → 小节），方便对照原文。
- FR-3.4：支持用户自定义压缩强度（保守 / 标准 / 激进），不同强度对应不同保留比例。
- FR-3.5：压缩不得捏造原文不存在的观点或例子（忠实原文是硬性要求）。

### 4.4 精华导出

- FR-4.1：将最终精华稿导出为 Markdown 文件（.md）。
- FR-4.2：导出文件包含元信息头（书名、作者、原格式、拆解日期、压缩比）。
- FR-4.3：支持导出预览与导出路径自定义。
- FR-4.4：同一本书可多次导出（每次导出独立文件，不覆盖历史版本）。

### 4.5 一键流程与过程可见性

- FR-5.1：用户导入书籍后，默认只需点击一次“开始拆解”，系统自动完成“无关内容识别与删减 → 核心章节压缩 → 精华提取 → 导出 MD”。
- FR-5.2：一键执行不隐藏内部过程；界面持续显示当前阶段、单书进度、已用时间、失败原因和任务状态。
- FR-5.3：每个阶段结束后展示基础统计信息，包括删除页数/字数、输入/输出字数和实际保留比例。
- FR-5.4：模型调用开始前展示预计输入/输出 token、API 调用量、处理时间和成本区间。

### 4.6 模型、密钥与云端确认

- FR-6.1：提供“设置”页面，用户可填写 / 修改 / 清除 DeepSeek API Key。
- FR-6.2：API Key 仅保存在本机（本地配置文件，权限受限），不出现在界面日志或导出文件中。
- FR-6.3：提供“测试连接”按钮，调用 DeepSeek API 验证密钥有效性并展示结果。
- FR-6.4：未配置密钥或密钥无效时，拆解任务应给出明确提示，不进入模型调用环节。
- FR-6.5：首次调用云端模型前，界面必须明确说明必要的正文片段会发送给 DeepSeek，并要求用户确认；用户拒绝时不得发起模型请求。

### 4.7 内容类型识别与告警

- FR-7.1：提取阶段至少识别正文中的表格、图片、公式和代码块。
- FR-7.2：系统当前不能可靠处理某类内容时，必须在界面和最终导出中显示内容类型、数量及所在章节或页面，不得静默忽略。
- FR-7.3：告警用于提示结果可能不完整，不自动触发复杂质量门禁，也不阻止用户导出已有结果。

### 4.8 持久化任务与恢复

- FR-8.1：多本书任务使用持久化队列；关闭页面或服务重启后，任务列表和队列顺序仍然保留。
- FR-8.2：每个可独立处理单元完成后保存检查点，恢复任务时从最近检查点继续。
- FR-8.3：支持取消等待中或运行中的任务；取消后保留已完成的有效中间结果。
- FR-8.4：支持恢复被取消、服务重启中断或临时失败的任务。
- FR-8.5：支持仅重试失败单元，不重复处理已经完成的单元。
- FR-8.6：对相同输入、配置、模型和处理单元复用缓存，避免不必要的重复 API 调用。
- FR-8.7：任务状态为“已完成”时，任务队列中的该任务必须展示“打开文件夹”按钮；等待中、运行中、已取消和失败任务不展示该按钮。
- FR-8.8：点击“打开文件夹”后，应使用任务结果中记录的实际导出路径，在 macOS Finder 中打开其所在文件夹并选中对应精华 Markdown，不得通过书名或固定目录猜测文件位置。
- FR-8.9：“打开文件夹”是只读操作，不得改变任务状态、重新执行拆解、重新导出或再次触发 MyDatabase 同步。
- FR-8.10：任务记录为已完成但导出文件已被用户移动、删除或当前不可访问时，界面应提示“导出文件不存在或无法访问”，不得无响应或打开无关目录。

### 4.9 任务与书库删除

#### 4.9.1 删除任务队列中的任务

- FR-9.1：任务队列中的每条任务均提供“删除任务”操作，适用于等待中、运行中、已取消、失败和已完成状态。
- FR-9.2：删除前必须二次确认，并展示书名、task ID、当前状态及删除范围；确认按钮应使用明确文案“删除任务”，不得仅使用“确定”。
- FR-9.3：删除等待中任务后，该任务不得再被调度；删除运行中任务时，系统先设置停止标记，不再创建新的模型请求，等待当前不可中断请求结束并保存安全边界后再删除任务。
- FR-9.4：删除任务应清理该任务的队列记录、检查点、失败信息和任务级中间状态；不得删除书库条目、用户导入前的原始文件、已导出的精华 MD 或 MyDatabase 条目。
- FR-9.5：删除任务默认保留可被其他任务复用的有效单元缓存，避免删除历史记录导致无必要的重复 API 成本。
- FR-9.6：删除完成后任务应立即从界面消失；刷新页面或重启服务后不得重新出现。对同一 task ID 重复发起删除应返回幂等结果，不产生 500 错误。

#### 4.9.2 删除书库中的书籍条目

- FR-9.7：书库中的每本书均提供“删除书籍”操作；删除前必须二次确认，并展示书名、book ID、关联任务数量及将被清理的数据范围。
- FR-9.8：存在等待中或运行中关联任务时，不得直接删除书籍；界面应说明阻塞任务并引导用户先删除对应任务，防止模型请求或导出流程读取到已删除书籍。
- FR-9.9：删除书籍条目应在一个受控操作中清理：书库记录、应用保存的导入副本、提取文本、元数据、删减/蒸馏中间产物、书籍级缓存，以及该书所有已取消、失败或已完成的关联任务记录。
- FR-9.10：删除书籍条目不得删除用户在 `/Users/hna/Downloads` 或其他导入来源中的原始文件；应用管理的文件在 macOS 上优先移入废纸篓或应用回收区，保留误删恢复可能。
- FR-9.11：既有精华 MD 导出和 MyDatabase 条目默认保留，因为它们属于独立交付物；本期“删除书籍”不提供联动删除外部交付物的选项。
- FR-9.12：删除成功后书籍应立即从书库消失，关联终态任务不得残留为孤儿记录；刷新页面或重启服务后不得恢复。对同一 book ID 重复发起删除应返回幂等结果。
- FR-9.13：删除数据库记录和关联关系必须使用事务；任一步骤失败时应返回可理解的错误，并保证书库和任务队列不会处于半删除状态。

## 5. 非功能需求

### 5.1 性能

- NFR-1.1：单本常规书籍（约 300 页）的完整拆解流程目标耗时不超过 5 分钟（不含用户确认和排队时间）。
- NFR-1.2：批量导入 10 本书时使用持久化队列，界面实时展示每本书的阶段和进度。
- NFR-1.3：开始前必须给出 token、调用量、时间和成本估算；估算应明确为区间，不得伪装成确定值。

### 5.2 兼容性

- NFR-2.1：支持 macOS（本项目开发环境），架构上不排除跨平台。
- NFR-2.2：输出的 MD 文件应兼容主流 Markdown 编辑器（Typora、Obsidian、VS Code 等）。
- NFR-2.3：“打开文件夹”在当前 macOS 环境中使用系统 Finder 定位文件；路径参数必须以参数形式安全传递，不得拼接执行任意 shell 命令。

### 5.3 可靠性

- NFR-3.1：任何一本书拆解失败时，应产出错误日志，且不影响队列中其他书籍。
- NFR-3.2：中间产物（删减稿、压缩稿）应保留在本地工作目录。
- NFR-3.3：任务状态、队列顺序、检查点和缓存必须持久化，服务重启不得把已完成任务误显示为未开始。
- NFR-3.4：恢复或重试不得重复创建已经成功完成的单元结果。
- NFR-3.5：删除任务或书籍时，数据库状态变更必须原子、幂等；应用崩溃或服务重启后不得出现孤儿任务、幽灵书籍或重复删除错误。
- NFR-3.6：删除运行中任务发起后，不得再创建新的 DeepSeek 请求；当前不可中断请求结束前，界面应显示“正在停止并删除”，不得提前显示删除成功。
- NFR-3.7：删除书库条目不得影响用户导入前的原始文件、已导出的精华 MD 和 MyDatabase 中已有内容。

### 5.4 质量

- NFR-4.1：拆解结果忠实于原文，不得引入原文没有的观点。
- NFR-4.2：拆解结果应保证“观点可读、例子具体”，而不是断章取义式的碎片。
- NFR-4.3：表格、图片、公式和代码块无法可靠处理时必须产生可见告警，但不要求本版自动理解或改写这些内容。

## 6. 边界与约束

- 输入：用户本地电子书文件。
- 输出：本地 MD 文件。
- 本地处理：文件保存、格式解析、内容删减、任务状态、检查点、缓存和最终导出在本机完成。
- 云端处理：压缩提炼调用 **DeepSeek V4 Flash API**，必要正文片段会发送给 DeepSeek；首次调用前必须明确提示并取得用户确认。
- 多模态边界：本版只要求识别表格、图片、公式和代码块，并在不能可靠处理时告警；不要求自动理解、描述或重建这些内容。
- 不涉及：DRM 破解、版权规避。仅处理用户合法拥有的书籍文件。

## 7. 关键决策点

1. **界面形态**：**已确认 —— 本地 Web 服务**。用户在本机通过浏览器访问。
2. **提炼引擎**：**已确认 —— DeepSeek V4 Flash 大模型 API**。API Key 由用户自行填写并保存在本机；必要正文片段会发送给 DeepSeek，首次调用前要求确认。
3. **压缩比与差异化**：**已确认 —— 目标保留原篇幅约 15%，按书籍类型差异化**：
   - 社科 / 商业 / 工具书：15% 左右。
   - 小说 / 文学：保留主线剧情与关键场景，比例可低于 15%。
   - 技术书：侧重方法与要点，比例可略高于 15%。
4. **小说类书籍**：**已确认 —— 采用独立策略**，保留主要人物、主线情节、关键转折和结局。
5. **笔记/批注功能**：**已确认 —— 不做**。
6. **任务模型**：**已确认 —— 持久化、可恢复的一键端到端任务**，内部阶段持续可见。
7. **删除边界**：**已确认 —— 任务与书籍分别删除**。删除任务不删除书籍和交付物；删除书籍清理应用内副本及关联终态任务，但不删除导入来源原文件、既有导出或 MyDatabase 内容。
8. **完成任务文件入口**：**已确认 —— 在任务队列提供“打开文件夹”按钮**，使用该任务的真实导出路径在 Finder 中定位文件，仅完成态可用。

## 8. 验收标准

### 8.1 基础体验

- AC-1：导入一本 EPUB 图书后，用户一次点击即可完成“删减 → 压缩 → 导出 MD”，过程中持续显示内部阶段和进度。
- AC-2：输出的 MD 中不包含版权页、目录、尾注、参考文献内容。
- AC-3：输出的 MD 保留原书全部核心章节的标题层级。
- AC-4：输出的 MD 中每个核心观点至少包含 1 个来自原文的例子。
- AC-5：输出 MD 的字数约为原文核心内容的 15% 左右，并按书籍类型差异化。
- AC-6：随机抽查 5 个观点，均能在原文中找到对应出处，且无捏造。

### 8.2 保留的 v0.2 改进

- AC-7：包含表格、图片、公式和代码块的测试书能识别这些内容；不能处理时，界面和导出均出现明确告警及位置。
- AC-8：开始拆解前显示 token、API 调用量、时间和成本区间；首次云端调用前明确说明正文片段会发送给 DeepSeek，用户拒绝后无模型请求产生。
- AC-9：批量提交 10 本书后，关闭页面再打开，队列顺序和任务状态仍然存在。
- AC-10：任务在完成至少一个处理单元后中断或重启，恢复后从检查点继续，不重复处理已完成单元。
- AC-11：取消运行中任务后可重新恢复；构造单元失败后，只重试失败单元。
- AC-12：使用相同输入、配置和模型重复执行时，已缓存单元不重复调用 DeepSeek。

### 8.3 删除能力

- AC-13：分别删除一条等待中、已取消、失败和已完成任务后，它们立即从任务队列消失，刷新页面和重启服务后不再出现；对应书库条目和既有导出仍存在。
- AC-14：删除一条运行中任务后，不再产生新的模型请求；界面先显示停止/删除过程，当前请求结束后任务消失，队列中的下一条任务可继续运行。
- AC-15：删除存在运行中关联任务的书籍时操作被阻止，并明确展示阻塞任务；删除关联任务后可继续删除书籍。
- AC-16：删除书库条目后，应用内副本、提取文本、元数据、中间产物、书籍级缓存和关联终态任务全部清理；用户导入来源原文件、既有精华 MD 和 MyDatabase 条目仍存在。
- AC-17：模拟删除过程中的文件移动或数据库写入失败，书籍和任务仍保持可理解、可重试的一致状态；对同一 task ID 或 book ID 重复删除返回幂等结果。

### 8.4 完成任务打开文件夹

- AC-18：任务进入“已完成”状态后，任务队列展示“打开文件夹”按钮；其他状态不展示。
- AC-19：点击按钮后，macOS Finder 打开实际导出文件所在文件夹并选中该任务对应的精华 Markdown；任务状态、导出文件数量和 MyDatabase 数据均不发生变化。
- AC-20：将导出文件移动或删除后再点击按钮，界面显示明确错误，且不打开固定输出目录或其他任务的文件夹。

## 9. 里程碑规划

1. **M1（已完成）**：确定技术方案并搭建本地 Web 服务骨架。
2. **M2（已完成）**：实现 EPUB/PDF/TXT/Markdown 导入与基础文本提取。
3. **M3（已完成）**：实现无关内容识别、删减、预览和误删恢复。
4. **M4（已完成）**：实现按章调用 DeepSeek 的压缩与观点提取。
5. **M5（已完成）**：实现精华 MD 导出和基础过程统计。
6. **M6（代码已完成）**：内容类型告警、持久化队列与检查点、取消/恢复/缓存/失败单元重试、一键流程、开始前估算和首次云端确认。
7. **M7（已完成）**：超出本版范围的产品行为和界面负担已回滚；自动化、10 本真实图书、重启恢复、质量抽检和约 300 页任务验收已通过。
8. **M8（已完成）**：实现任务删除、书库条目删除和完成任务“打开文件夹”；停止边界、依赖检查、事务清理、可恢复文件删除、真实导出路径定位、幂等接口和 AC-13 至 AC-20 回归均已验收。

## 10. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 大模型提炼时捏造观点 | 破坏忠实性 | 提示词约束，并按 AC-6 随机抽检原文 |
| 不同格式提取质量参差 | 删减或压缩质量下降 | EPUB/TXT/MD 优先，PDF 分级支持并显示失败原因 |
| 表格、图片、公式或代码块被静默忽略 | 技术书关键内容缺失 | 识别内容类型；不能处理时显示位置和告警 |
| 小说类书籍不适合“观点 + 例子”模型 | 输出质量差 | 使用独立的剧情与关键场景策略 |
| 压缩比过猛导致上下文不足 | 用户读不懂 | 提供多档压缩强度和预览确认 |
| 服务重启或临时错误导致从头开始 | 重复等待和重复 API 成本 | 持久化队列、检查点、缓存、恢复和失败单元重试 |
| 时间或成本估算不准 | 用户无法判断是否开始 | 展示估算区间，避免给出伪精确数字 |
| 云端数据流说明不清 | 用户误以为正文不离开本机 | 首次调用前明确说明发送必要正文片段并要求确认 |
| 误删任务时连带丢失书籍或导出 | 用户无法恢复已有成果 | 任务删除严格限定为任务记录与任务级状态，保留书库、导出和共享缓存 |
| 删除运行中任务后模型仍继续调用 | 继续产生时间和 API 成本 | 先持久化停止标记，不再创建新请求；当前请求结束后再完成删除 |
| 删除书籍产生孤儿任务或半删除文件 | 书库与队列状态不一致 | 活跃任务前置阻塞、数据库事务、幂等接口和失败回滚；应用内文件优先可恢复删除 |
| “打开文件夹”使用固定路径或文件名猜测 | 打开错误文件，用户误以为导出丢失 | 只使用任务结果保存的绝对导出路径；文件不存在时明确报错 |
