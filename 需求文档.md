# 图书拆解器 —— 需求文档

- 版本：v0.3（P0/P1/P2 实现版）
- 日期：2026-08-05
- 状态：P0/P1/P2 代码实现与自动化回归已完成；真实金标、破坏恢复和性能验收待完成

## 0. 实现与验收状态

| 范围 | 已实现 | 尚待真实验收 |
| --- | --- | --- |
| P0 | 结构/覆盖检查、EPUB 同页多标题修复、可逆删减映射、模态清单、结构化知识单元、逐字引文锚点、全书去重、质量失败阻断、诊断稿 | 真实 EPUB/PDF/TXT/MD 金标集；至少 10 条核心知识人工忠实性抽检；复杂表格/公式/图片夹具扩充 |
| P1 | 全书定向扫描、`relevance/novelty/dependency/evidence_value`、全书总预算、自适应分配、预算偏差门禁、锚定知识全书合并 | 使用真实 DeepSeek 输出验证保留比例、关键机制/前置章节预算优势与术语一致性 |
| P2 | SQLite 队列与检查点、独立 `run_id`、一键流水线、连接复用、单书 3 并发/书籍 2 并发、退避重试、精确缓存键、取消、恢复、失败单元重试、费用/时间估算与实际统计 | 10 本书队列破坏测试；服务强制重启恢复；300 页参考书 P50/P95；调用量降低 30% 与估算误差报告 |

当前自动化基线为 **86 个测试通过**。该数字证明代码回归基线，不替代 AC-P0-5、AC-P2-7、AC-P2-8 等真实验收。

## 1. 项目概述

### 1.1 背景

现代人的阅读时间碎片化，而图书中往往包含版权页、目录、广告、重复叙述和其他结构性附属内容；脚注、尾注、参考文献等内容虽然不一定适合进入主阅读路径，却可能承载关键证据。用户希望在不静默遗漏核心知识和证据的前提下，大幅缩短阅读时间。

### 1.2 产品定位

图书拆解器是一个**可追溯的书籍精华提取工具**：将用户导入的电子书先做结构与覆盖校验，再删减无关内容；通过全书定向扫描识别知识价值并动态分配压缩预算，最后输出经过全书去重、原文锚点校验的精华版 Markdown 文档。

产品优化目标从“把书压缩到固定比例”调整为“在可控的总时间和成本内，最大化用户获得的、可核验的核心知识”。压缩比例仍用于约束阅读与调用成本，但不能替代完整性、唯一性和忠实性验收。

### 1.3 目标

- 帮助用户节约阅读时间。
- 提高知识获取效率。
- 保证核心知识不因解析、切分、删减或预算分配而静默丢失。
- 合并全书重复观点，同时保留全部来源位置。
- 使重要观点、数据和例子能够回到原文核验。
- 减少不必要的模型调用、重复处理和人工操作，支持任务中断后继续。
- 产出可长期保存、可检索、可分享的精华文档（MD 格式）。

### 1.4 非目标（本期不做）

- 不做书籍翻译。
- 不做全文检索 / 知识图谱。
- 不做多本书的横向对比总结。
- 不做社交分享功能。
- 不做笔记 / 批注 / 书签功能（精华稿的笔记请用户在外部工具中自行完成）。
- 本次 v0.2 不新增题库、记忆训练、间隔复习或互动教学功能。

## 2. 目标用户

| 用户类型 | 典型场景 |
| --- | --- |
| 忙碌的知识工作者 | 需要在短时间内掌握一本书的核心观点 |
| 学生 / 备考者 | 快速回顾教材或参考书的要点 |
| 泛读爱好者 | 大量浏览书籍，只对少数书精读 |

## 3. 术语定义

- **无关内容**：经规则识别、证据关系检查或用户确认后，确定不承载核心观点、必要前提或关键证据的部分，如版权页、广告页、空白页和重复装饰内容。目录用于建立结构但不进入精华正文；序言、脚注、尾注、参考文献和索引不能仅凭类型直接判定为无关。
- **核心章节**：承载书籍主旨论述的正文章节。
- **核心观点**：作者的核心论点、结论、方法论，以及支撑论点的关键论据。
- **关键例子**：最能说明论点、最具说服力或最生动的案例、故事、数据。
- **核心正文**：剔除经用户确认的无关区域后，进入价值评估与提炼的全部有效内容。
- **正文覆盖率**：实际进入提炼单元的核心正文字符并集 / 核心正文总字符数；重复覆盖只计算一次。
- **原文锚点**：能从精华结论返回原文的稳定位置，至少包含章节/页码或字符区间，以及用于核验的短文本片段或内容指纹。
- **知识单元**：精华稿中的独立观点、事实、方法、数据、例子或情节节点，具有唯一 ID、类型和来源锚点。
- **重复知识**：语义实质相同、只因章节复述或切片重叠而重复出现的知识单元。
- **定向扫描**：正式压缩前对全书目录、章节摘要和关键证据区域进行的第一遍分析，用于建立章节角色和压缩预算。
- **自适应预算**：在全书总压缩预算内，根据相关性、新颖度、前置依赖和证据价值为不同章节/片段分配不同目标篇幅。
- **质量门禁**：结构、覆盖、锚点、去重等指标不达标时，阻止任务进入下一阶段或阻止结果被标记为完成的硬性检查。
- **任务检查点**：任务在每个可独立重试单元完成后持久化的状态，用于取消、重启恢复和断点续跑。
- **精华版 MD**：本项目最终产出的 Markdown 文档。

## 4. 功能需求

### 4.1 书籍导入

- FR-1.1：支持导入常见电子书格式：EPUB、PDF、TXT、Markdown。
- FR-1.2：导入后展示书籍基本信息（书名、作者、格式、页数/字数、导入时间）。
- FR-1.3：支持批量导入多本书（队列处理）。
- FR-1.4：导入失败的书籍需给出明确失败原因（如格式不支持、文件损坏）。
- FR-1.5：导入完成后计算源文件内容指纹，用于识别重复导入、复用中间结果和关联历史版本。

### 4.2 无关内容删减

- FR-2.1：自动识别并剔除版权页、出版信息页。
- FR-2.2：自动识别并剔除目录页（生成书签用，不进入正文精华）。
- FR-2.3：自动识别脚注、尾注、参考文献和索引，但不得一律删除。它们默认不占用主叙述预算；被核心观点引用、包含关键定义/数据/限制条件或用于原文核验时，应保留在证据清单并建立锚点。
- FR-2.4：自动识别并剔除空白页、重复页、广告页。
- FR-2.5：序言、导言、后记是否保留，可由用户选择（默认保留序言/导言，剔除后记以外的附属内容）。
- FR-2.6：删减结果应可预览，用户可手动撤销误删（将某段内容恢复到精华稿中）。
- FR-2.7：每个删除区域必须记录原因、原文区间、识别规则和恢复状态；删除区域仍可被来源锚点定位，不得物理破坏原始文件。

### 4.3 核心内容压缩

- FR-3.1：压缩预算以**全书核心正文**为计算口径。标准模式的总目标仍为约 15%，但不要求每章保留相同比例；保守 / 标准 / 激进档位约束的是全书总预算。
- FR-3.2：正式压缩前执行一次全书定向扫描，识别核心问题、章节角色、概念依赖、主要证据区域和重复区域。
- FR-3.3：为章节或片段计算并持久化以下预算因子：
  - `relevance`：与全书核心问题的相关性。
  - `novelty`：相对前文是否提供新的观点、证据或方法。
  - `dependency`：是否是理解后续内容所需的前置知识。
  - `evidence_value`：是否包含关键数据、案例、公式、实验或论证。
- FR-3.4：在全书总预算内动态分配目标篇幅。高依赖、高证据价值内容优先保留；背景、重复论述和低信息密度内容获得更少预算。系统必须记录原始评分、分配结果和调整原因，供用户查看。
- FR-3.5：压缩后保留：
  - 核心观点及其逻辑关系（论点 → 论据 → 结论）。
  - 原文存在且能有效说明观点时，保留最关键、最生动的代表例子；原文没有例子时不得为了满足格式而编造。
  - 关键术语及其首次出现时的简短解释。
  - 对理解结论必需的前提、边界、反例和限制条件。
- FR-3.6：压缩结果应保留原文的章节层级和原文锚点，允许从知识单元展开到对应原文。
- FR-3.7：完成所有章节提炼后，必须进行一次全书级合并：统一术语、连接跨章节逻辑、合并语义重复知识，并在合并后的知识单元中保留全部来源锚点。
- FR-3.8：支持用户自定义压缩强度（保守 / 标准 / 激进）。系统可以在质量门禁要求下小幅偏离目标比例，但必须说明原因，不得为了命中比例删除必要证据或前置知识。
- FR-3.9：不得捏造原文不存在的观点、数据、例子、人物或情节。系统生成的跨章节综合判断必须标记为 `[推断]`，并列出支撑它的原文锚点；无法核验的内容不得作为核心知识输出。
- FR-3.10：预算分配、模型版本、提示词版本、输入单元范围和输出单元关系必须写入中间清单，以支持复现、重试和质量审计。

### 4.4 精华导出

- FR-4.1：将最终精华稿导出为 Markdown 文件（.md）。
- FR-4.2：导出文件包含元信息头（书名、作者、原格式、源文件指纹、拆解日期、模型与提示词版本、压缩强度、实际压缩比）。
- FR-4.3：支持导出预览与导出路径自定义。
- FR-4.4：同一本书可多次导出（每次导出独立文件，不覆盖历史版本）。
- FR-4.5：导出文件包含质量摘要：正文覆盖率、原文锚点覆盖率、无法解析内容、重复合并数、失败/告警单元数和未解决问题。
- FR-4.6：质量门禁未通过时只允许导出带明显“未通过质量校验”标记的诊断稿，不得将其命名或展示为正式精华稿。

### 4.5 过程可见性

- FR-5.1：默认以一个端到端任务展示进度：导入 → 结构/覆盖校验 → 删减 → 定向扫描 → 预算分配 → 分段提炼 → 全书合并/去重 → 锚点校验 → 导出。
- FR-5.2：每个阶段结束后展示统计信息，包括输入/输出字符数、覆盖率、预算使用、API 调用数、缓存命中数、已用时间和预计剩余时间。
- FR-5.3：界面必须明确显示当前输入是原始正文还是删减稿，以及各质量门禁的通过/告警/失败状态；不得静默跳过删减或校验阶段。
- FR-5.4：开始模型调用前展示预计处理时间、输入/输出 token、API 调用量和成本区间，允许用户确认或取消。

### 4.6 模型与密钥配置

- FR-6.1：提供"设置"页面，用户可填写 / 修改 / 清除 DeepSeek API Key。
- FR-6.2：API Key 仅保存在本机（本地配置文件，权限受限），不出现在界面日志或导出文件中。
- FR-6.3：提供"测试连接"按钮，调用 DeepSeek API 验证密钥有效性并展示结果。
- FR-6.4：未配置密钥或密钥无效时，拆解任务应给出明确提示，不进入模型调用环节。
- FR-6.5：首次调用云端模型前，界面必须明确说明必要的正文片段会发送给 DeepSeek，并让用户确认；不得使用“全部数据仅在本机处理”等与实际数据流不一致的文案。

### 4.7 知识质量门禁（P0）

- FR-7.1：提炼前检查章节/片段区间：起止偏移合法、顺序单调、无意外重叠、无零长度核心章节；异常必须定位到具体格式、文件和章节。
- FR-7.2：系统必须计算核心正文覆盖率、未覆盖区间和重复覆盖区间。低于质量阈值时停止模型调用，允许用户查看问题和重新提取。
- FR-7.3：提取阶段必须建立内容模态清单，至少识别正文、标题、表格、图片、公式、代码块和脚注。当前无法可靠保留或理解的模态必须记录来源位置并告警，不得静默丢弃。
- FR-7.4：每个核心知识单元必须包含唯一 ID、知识类型、内容、来源锚点和核验状态。知识类型至少区分 `[事实]`、`[来源主张]`、`[推断]`、`[假设]`、`[未知]`。
- FR-7.5：系统必须自动验证锚点是否存在、是否落在合法原文区间、短文本片段是否能与原文匹配。核心知识单元没有有效锚点时，质量门禁失败。
- FR-7.6：系统必须在切片内、章节间和全书级三个层次检测重复知识。合并重复知识时不得丢弃独立证据，应把多个出处归并到同一知识单元。
- FR-7.7：系统必须保存“原文 → 删减稿 → 提炼单元 → 知识单元 → 导出段落”的映射清单，使压缩和去重过程可追溯。
- FR-7.8：模型输出长度符合预算不能替代质量检查。只有结构、覆盖、锚点、去重和错误检查全部通过，任务才能标记为完成。
- FR-7.9：提供格式夹具、人工金标样本和端到端自动化测试，覆盖 EPUB 同页多标题、删减后偏移、切片重叠、扫描版 PDF、表格/公式/图片告警及模型捏造检测。

### 4.8 任务效率与恢复（P2）

- FR-8.1：用户默认只需发起一次任务；系统自动编排删减、预算分配、提炼、校验和导出。高级阶段控制不得增加普通用户的必需操作数。
- FR-8.2：任务状态和每个提炼单元的检查点必须持久化。关闭页面或服务重启后，用户可以继续未完成任务，无需重新处理已完成单元。
- FR-8.3：支持取消任务。取消后停止创建新的模型请求，保留已完成的有效中间产物，并清晰展示是否存在尚未结束的在途请求。
- FR-8.4：支持仅重试失败单元。已通过质量门禁的单元不得重复调用模型，除非输入、参数、模型或提示词版本发生变化。
- FR-8.5：缓存键至少包含源文件指纹、删减配置、书籍类型、压缩强度、输入区间、模型版本和提示词版本；缓存命中与失效原因对用户可见。
- FR-8.6：模型调用复用网络连接，采用有上限的并发、超时、指数退避和针对限流/临时错误的安全重试；并发度不得破坏章节顺序、预算或质量门禁。
- FR-8.7：支持多本书任务队列；单本失败不得阻塞其他书，用户可调整等待中任务的顺序或取消任务。
- FR-8.8：所有性能优化必须在同一金标集上重新通过 P0 门禁。出现知识覆盖、锚点有效率或去重质量退化时不得发布。

## 5. 非功能需求

### 5.1 性能

- NFR-1.1：在固定参考环境、同一模型和同一金标书籍上，约 300 页常规书籍的标准模式端到端处理时间目标为 P50 不超过 5 分钟、P95 不超过 8 分钟（不含用户确认和排队时间）。每次验收必须保存真实耗时与环境信息，不以估算代替。
- NFR-1.2：批量导入 10 本书时支持持久化任务队列，界面实时展示每本书的阶段、进度、等待原因、已用时间和预计剩余时间。
- NFR-1.3：在 P0 质量指标不退化的前提下，参考书籍的模型请求次数相对 M4 基线降低至少 30%；基线与改进结果必须使用相同输入、配置和模型版本。
- NFR-1.4：用户取消任务后 3 秒内停止创建新的模型请求；已经发出的在途请求需要明确标记并在返回后丢弃或按用户选择保留。
- NFR-1.5：服务重启后 5 秒内恢复任务清单和最后检查点，不得重复调用已完成且缓存键未变化的单元。
- NFR-1.6：开始前的 token/成本估算与实际值偏差不超过 15%；时间估算在 API 服务正常时偏差目标不超过 30%。

### 5.2 兼容性

- NFR-2.1：支持 macOS（本项目开发环境），架构上不排除跨平台。
- NFR-2.2：输出的 MD 文件应兼容主流 Markdown 编辑器（Typora、Obsidian、VS Code 等），原文锚点和质量摘要不得依赖专有渲染器才能读取。

### 5.3 可靠性

- NFR-3.1：任何一本书拆解失败时，应产出错误日志，且不影响队列中其他书籍。
- NFR-3.2：中间产物（删减稿、压缩稿）应保留在本地工作目录，可追溯。
- NFR-3.3：任务状态、中间清单和最终结果应采用原子写入或等价机制，进程异常退出不得留下看似完成但内容不完整的文件。
- NFR-3.4：同一本书重新运行时不得静默覆盖旧的提炼结果、预算清单或质量报告；每次运行具有独立 ID，并可追溯到源文件指纹和配置版本。
- NFR-3.5：重试必须幂等。一个单元成功写入后，即使客户端重复提交也不得造成重复知识单元或重复计费调用。

### 5.4 质量

- NFR-4.1：核心知识单元的有效原文锚点覆盖率必须为 100%；无法锚定的模型内容只能标记为 `[未知]` 并进入问题清单，不得进入正式精华正文。
- NFR-4.2：核心正文覆盖率必须不低于 99%。被删减或无法解析的区间不计入分子，但必须在质量报告中逐项列明；未列明的遗漏视为门禁失败。
- NFR-4.3：进入提炼的章节区间必须全部满足起止合法、顺序单调、无零长度核心章节和无意外重叠。
- NFR-4.4：金标重复样本中的同义复述在正式精华稿中只生成一个知识单元，同时保留所有独立来源锚点。
- NFR-4.5：人工随机抽检至少 10 个核心知识单元，观点、数字、人物、例子和因果方向均能在对应原文中得到支持，不得出现捏造。
- NFR-4.6：表格、图片、公式、代码块等模态在格式夹具中的识别率必须为 100%；不能可靠处理时必须告警并阻止“无告警通过”。
- NFR-4.7：全书实际保留比例应落在所选强度目标的 ±3 个百分点内；若为通过 P0 门禁而超出，质量报告必须说明超出原因和具体保留内容。
- NFR-4.8：拆解结果应保证“观点可读、例子具体、前提和边界可见”，而不是断章取义式的碎片。

## 6. 边界与约束

- 输入：用户本地电子书文件。
- 输出：本地 MD 文件。
- 处理方式：文件保存、格式解析、结构校验、规则删减、任务状态和最终导出在本机完成；压缩提炼调用 **DeepSeek V4 Flash API**，因此必要的正文片段会发送给 DeepSeek。API Key 由用户在界面中自行填写并保存在本机，调用内容、调用次数和成本应对用户透明。
- 多模态边界：P0 至少要求识别并记录表格、图片、公式和代码块的位置。某类内容尚不能可靠理解时，系统应保留来源锚点并告警；本期不以不可靠的自动描述填补缺失内容。
- 质量优先级：P0 是 P1/P2 的发布前置条件；不得为了命中压缩比例、缩短时间或减少调用而绕过 P0 门禁。
- 不涉及：DRM 破解、版权规避。仅处理用户合法拥有的书籍文件。

## 7. 关键决策点

以下问题影响技术方案。已确认项直接生效，未确认项需在开发前与用户确认：

1. **界面形态**：**已确认 —— 本地 Web 服务**。在用户本机启动 Web 服务，通过浏览器访问；本地阶段不外传数据，云端提炼阶段按任务需要把正文片段发送给 DeepSeek，并在首次调用前提示用户。
2. **提炼引擎**：**已确认 —— DeepSeek V4 Flash 大模型 API**。核心压缩与观点提取由 DeepSeek API 完成，**API Key 由用户自行填写**（密钥仅保存在本机，不随项目上传或外发）。模型输出必须经过程序化锚点校验和人工金标验收，不能只依赖提示词。
3. **压缩比与差异化**：**已确认 —— 全书总预算 + 自适应分配**。标准模式以全书核心正文约 15% 为目标，不要求逐章等比例；最终分配由相关性、新颖度、前置依赖和证据价值决定。初步总预算建议：
   - 社科 / 商业 / 工具书：标准模式约 15%，优先保留观点、逻辑、证据、边界与例子。
   - 小说 / 文学：保留主线剧情与关键场景，总比例可低于 15%。
   - 技术书：侧重前置概念、方法、公式、参数和可操作要点，总比例可高于 15%。
4. **小说类书籍**：**已确认 —— 采用独立策略**。不适用"核心观点 + 例子"模型，改为保留**主线剧情与关键场景**（主要人物、核心情节脉络、高潮/转折场景、结局），比例可低于 15%。
5. **笔记/批注功能**：**已确认 —— 不做**。用户不在此工具内追加笔记，仅导出精华 MD 供外部使用。
6. **质量门禁**：**已确认 —— P0 先于 P1/P2**。章节结构、正文覆盖、来源锚点、全书去重和无法解析内容告警未通过时，任务不得标记为成功。
7. **任务模型**：**已确认 —— 持久化、可恢复的端到端任务**。普通用户一次发起即可；系统内部按可重试单元执行，并支持取消、断点续跑和缓存复用。

## 8. 验收标准

### 8.1 基础流程

- AC-1：导入一本受支持格式的书籍后，用户一次发起即可完成“校验 → 删减 → 定向扫描 → 预算分配 → 提炼 → 全书合并/去重 → 锚点校验 → 导出”。
- AC-2：输出的正式精华稿不包含经确认删除的版权页、目录、广告等内容；所有删除区域均可在中间清单中定位并恢复。
- AC-3：正式导出的 MD 包含元信息、精华正文、来源锚点和质量摘要，历史版本不被覆盖。

### 8.2 P0：知识不丢、不重、不编

- AC-P0-1：EPUB 格式夹具在同一 XHTML 文件中包含多个层级标题，提取后所有核心章节起止合法、顺序单调、无零长度章节和意外重叠。
- AC-P0-2：包含章节内删除区域的格式夹具经过删减后，章节新区间与删减稿完全对应，相邻章节不重叠、不漏切。
- AC-P0-3：所有参考书籍的核心正文覆盖率不低于 99%；每个未覆盖区间都在质量报告中有明确原因和原文位置。
- AC-P0-4：核心知识单元的有效原文锚点覆盖率为 100%；程序逐条验证锚点区间与核验片段存在且匹配。
- AC-P0-5：人工随机抽检至少 10 个核心知识单元，观点、数据、人物、例子和因果方向均受原文支持，无捏造或立场反转。
- AC-P0-6：包含跨章节同义复述和切片重叠的金标样本，正式精华稿只保留一个知识单元，并在该单元中保留全部独立出处。
- AC-P0-7：包含表格、图片、公式和代码块的格式夹具能 100% 识别这些模态；不能可靠处理时，导出和界面均显示对应告警和来源位置。
- AC-P0-8：任一结构、覆盖、锚点或无法解析内容的硬门禁失败时，任务状态为“质量校验失败”，不得显示“拆解完成”或生成无警告的正式精华稿。
- AC-P0-9：自动化测试至少覆盖 EPUB、PDF、TXT、Markdown 四类输入，以及章节偏移、删减重映射、切片重叠、锚点验证、全书去重和失败阻断路径。

### 8.3 P1：自适应压缩预算

- AC-P1-1：每个提炼单元都保存 `relevance`、`novelty`、`dependency`、`evidence_value` 四项评分、最终预算和分配理由。
- AC-P1-2：在包含“关键机制章节、必要前置章节、重复章节、背景章节”的金标书籍中，关键机制和必要前置章节获得的单位字符预算高于重复与背景章节。
- AC-P1-3：全书实际保留比例落在所选档位目标的 ±3 个百分点内；因 P0 门禁超出时，质量报告能定位具体原因与内容。
- AC-P1-4：章节提炼完成后，全书合并阶段能统一同义术语、连接跨章节逻辑，并在不丢失独立证据的前提下消除重复知识。
- AC-P1-5：切换书籍类型或压缩强度后生成新的预算清单；未变化且缓存键一致的定向扫描结果可以复用。

### 8.4 P2：等待时间与操作成本

- AC-P2-1：普通用户从书库发起任务后无需依次点击“删减”和“拆解”；界面持续展示所有内部阶段及质量状态。
- AC-P2-2：任务运行中点击取消，3 秒内不再创建新模型请求；已完成中间产物和在途请求状态清晰可见。
- AC-P2-3：在任意提炼单元完成后强制重启服务，5 秒内恢复任务和最后检查点，继续后不重复调用已完成且缓存键未变化的单元。
- AC-P2-4：构造一个单元失败场景，点击重试只重新处理失败单元，其他已通过单元的内容、锚点和调用计数不变。
- AC-P2-5：同一参考书籍、相同模型和参数下，优化后模型请求次数相对 M4 基线降低至少 30%，且 P0 全部质量指标不退化。
- AC-P2-6：开始前展示 token、API 调用量、成本和时间估算；完成后展示实际值，偏差满足 NFR-1.6。
- AC-P2-7：批量提交 10 本书后，队列顺序、单书进度、失败原因、取消和继续均可操作；单本失败不阻塞其余任务。
- AC-P2-8：约 300 页参考书在固定环境下的真实端到端耗时满足 NFR-1.1，并保存可复查的测试报告。

## 9. 里程碑规划

1. **M1（已完成）**：确定技术方案并搭建本地 Web 服务骨架。
2. **M2（已完成）**：实现 EPUB/PDF/TXT/Markdown 导入与基础文本提取。
3. **M3（基础版已完成）**：实现规则删减、预览和误删恢复。
4. **M4（基础版已完成）**：实现按章调用 DeepSeek 的压缩与观点提取。
5. **M5（代码已完成）**：实现精华 MD 导出和基础过程统计。
6. **M6（P0，代码已完成）**：修复章节/删减偏移；实现覆盖检查、模态清单、知识单元、原文锚点、全书去重、质量报告和自动化门禁；真实金标验收待完成。
7. **M7（P1，代码已完成）**：实现全书定向扫描、四因子评分、自适应预算和全书级合并；真实模型预算验收待完成。
8. **M8（P2，代码已完成）**：实现端到端任务编排、连接复用、有限并发、估算、缓存、取消、检查点、恢复和失败单元重试；真实队列/性能验收待完成。
9. **M9（端到端验收，未完成）**：依次通过 AC-P0、AC-P1、AC-P2；P0 未通过时不得验收或发布 P1/P2。

## 10. 附：风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| EPUB 标题偏移或删减后区间映射错误 | 整章漏读、重复输入或章节错切 | 结构门禁 + 格式夹具 + 正文覆盖/重叠报告；失败时阻止模型调用 |
| PDF、EPUB 中的表格、图片、公式或代码布局被纯文本提取静默丢弃 | 关键证据和技术知识缺失 | 建立模态清单；不能处理时保留锚点并告警，不用模型猜测缺失内容 |
| 大模型捏造观点、数据、例子或改变作者立场 | 正式精华稿不可信 | 知识单元结构化输出 + 100% 锚点校验 + 金标抽检 + 未通过阻断 |
| 切片重叠或作者跨章节复述造成重复知识 | 增加阅读时间并夸大观点重要性 | 切片内、章节间、全书级三层去重；合并时保留全部独立出处 |
| 自适应预算错误地压缩前置知识或反例 | 用户只看到结论，无法理解适用边界 | 将 dependency、evidence_value 纳入评分；金标预算测试；允许 P0 触发预算上调 |
| 过度追求 15% 导致核心证据丢失 | 精华稿短但不可理解或不可核验 | 把 15% 定义为总目标而非硬上限；P0 门禁优先并记录偏离原因 |
| 并发、缓存或大切片优化使质量退化 | 速度提高但遗漏、重复或错配增加 | 所有 P2 变更必须在相同金标集重新通过 P0，并与基线对比 |
| 服务重启或网络错误导致任务从头开始 | 重复等待和重复 API 成本 | 持久化检查点、幂等单元、精确缓存键和仅重试失败单元 |
| 时间或成本预估不准 | 用户无法决定是否开始或继续 | 保存估算与实际数据，持续校准模型；超过误差阈值时显示低可信度提示 |
| 云端提炼的数据流说明不清 | 用户误以为全部内容只在本机处理 | 首次调用前明确告知发送范围，过程统计记录调用单元和次数 |
| 小说类书籍不适合“观点 + 例子”模型 | 输出质量差 | 保持小说独立策略和金标样本，不与知识类书籍共用预算评分权重 |
